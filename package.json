{
  "name": "civicpulse",
  "version": "1.0.0",
  "description": "A web app for citizens to report local issues and hold officials accountable",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "npm run install-all && cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "npm run dev",
    "test-setup": "node test-setup.js",
    "postinstall": "npm run install-all"
  },
  "keywords": ["civic", "accountability", "local-government", "citizen-engagement"],
  "author": "CivicPulse Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
